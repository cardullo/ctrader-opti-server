# Minimal smoke-test job config — 9 passes (3×3) over a short period.
# Use this FIRST to verify the full pipeline works end-to-end before
# running a real 200-500 pass optimization.

name: EMA_v2.2_smoke_test
symbol: EURUSD
period: H1
start: "01/01/2024"
end: "01/06/2024"
data_mode: m1
balance: 10000
commission: 15
spread: 1
strategy: grid
max_passes: 9
parallel_workers: 2
fitness: net_profit
params:
  FastPeriod:
    min: 5
    max: 15
    step: 5
  SlowPeriod:
    min: 20
    max: 40
    step: 10
